import {StyleSheet} from 'react-native';

const ForgotPassword = StyleSheet.create({
  backIconContainer: {
    position: 'absolute',
    left: 0,
  },
  verifyMyMailButtonContainer: {marginTop: 20, marginBottom: 16},
});

export default ForgotPassword;
