import {StyleSheet} from 'react-native';

const SavingsRecommenderPreviewBills = StyleSheet.create({
  skeletonToggler: {width: '92.5%', alignSelf: 'center'},
  imageLoader: {
    position: 'absolute',
    left: 0,
    top: 0,
    right: 0,
    bottom: 0,
  },
});

export default SavingsRecommenderPreviewBills;
