import {StyleSheet} from 'react-native';

const ImagePicker = StyleSheet.create({
  imagePickerModalConatiner: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    paddingVertical: 8,
  },
});

export default ImagePicker;
