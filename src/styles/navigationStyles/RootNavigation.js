import {StyleSheet} from 'react-native';
import Colors from '../../utils/Colors';

const RootNavigation = StyleSheet.create({
  logoContainer: {
    width: 48,
    height: 48,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: Colors.white,
    borderRadius: 48 / 2,
  },
});

export default RootNavigation;
