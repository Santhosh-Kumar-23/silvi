import {StyleSheet} from 'react-native';

const EarningView = StyleSheet.create({
  headerIconContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 4,
    paddingVertical: 4,
  },
});

export default EarningView;
