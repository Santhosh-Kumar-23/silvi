import {StyleSheet} from 'react-native';

const SpendingView = StyleSheet.create({
  headerIconContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 4,
    paddingVertical: 4,
  },
});

export default SpendingView;
